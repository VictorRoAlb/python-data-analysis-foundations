# Figures

This folder stores the public-safe visuals used in the README and in the portfolio website.

The source datasets are not included here. Figures can be regenerated locally by setting:

- `SLEEP_STUDY_SOURCE`
- `TOYOTA_SOURCE`

and running the scripts in `src/`.
